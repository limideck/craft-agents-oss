/** Open Connector console sections (workbench Connectors module). */
export type OpenConnectorSection = 'overview' | 'providers' | 'actions' | 'runs' | 'access'

export const OPEN_CONNECTOR_SECTIONS: OpenConnectorSection[] = [
  'overview',
  'providers',
  'actions',
  'runs',
  'access',
]
